# Scope: TypeScript Configuration

## Overview
`@labs/typescript-config` contains shared `tsconfig.json` files to standardize TypeScript compiler options across the monorepo.

## Configurations
- `nextjs.json`: For Next.js applications
- `react-library.json`: For React component libraries
- `base.json`: Base configuration
