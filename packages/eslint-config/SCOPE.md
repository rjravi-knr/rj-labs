# Scope: ESLint Configuration

## Overview
`@labs/eslint-config` provides shared ESLint configurations to ensure code quality and consistency across the monorepo.

## Configurations
- `next.js`: For Next.js applications
- `react-internal`: For internal React packages
- `base`: Base configuration for generic JavaScript/TypeScript
